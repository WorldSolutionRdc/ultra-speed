worker_processes auto;

events {
    worker_connections 1024;
}

stream {
    upstream backend {
        server 31.59.120.101:443;
    }
    
    server {
        listen 8080;
        proxy_pass backend;
        proxy_connect_timeout 30s;
        proxy_timeout 30m;
        tcp_nodelay on;
    }
}
